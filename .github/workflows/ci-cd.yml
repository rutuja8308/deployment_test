name: Build and Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build WAR
        run: mvn clean package

      - name: Rename WAR
        run: |
          if exist target\Project-0.0.1-SNAPSHOT.war (
            rename target\Project-0.0.1-SNAPSHOT.war Project.war
          )

      - name: Deploy to Tomcat
        run: copy target\Project.war "C:\Program Files\Apache Software Foundation\Tomcat 10.1\webapps\"

      - name: Restart Tomcat
        run: |
          net stop Tomcat10
          net start Tomcat10
